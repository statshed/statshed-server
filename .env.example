# StatShed server configuration.
# Copy to .env and customize:   cp .env.example .env

# --- Ports exposed on the host ---
# Web UI (the dashboard). Open http://localhost:${FRONTEND_PORT} in a browser.
FRONTEND_PORT=7827
# Backend REST API + WebSocket. CLI clients (statshed) POST job status here.
BACKEND_PORT=7828

# --- Backend ---
# Flask secret key. Generate a strong value for any shared/non-local deployment:
#   openssl rand -hex 32
# If left empty, a random key is generated on each start (fine for throwaway local use).
SECRET_KEY=

# Database. Default is SQLite persisted in the "statshed-data" Docker volume.
# For multi-instance / multi-worker deployments use PostgreSQL, e.g.:
#   DATABASE_URL=postgresql://user:pass@host:5432/statshed
DATABASE_URL=sqlite:////data/statshed.db

# Flask debug mode. Keep false outside local development.
DEBUG=false

# Origins allowed to call the API from a browser. Unused in the default bundled
# deployment (the UI is same-origin via nginx); only matters if you point a browser
# at the backend port directly. Comma-separated.
# CORS_ORIGINS=http://localhost:7827

# --- Releases (docker-compose.release.yml only) ---
# Image tag to pull from ghcr.io/statshed. Use a released version like v0.1.0,
# or "latest" for the most recent published build.
STATSHED_VERSION=latest
